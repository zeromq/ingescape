#!/bin/sh

#Due to privileges limitations in Homebrew, we cannot
#execute automatic removal of brew-managed zyre and dependencies
#automatically.
#FILE=/usr/local/bin/brew
#if [[ -f "$FILE" ]]; then
#    brew remove -f zyre czmq zeromq libsodium
#fi
